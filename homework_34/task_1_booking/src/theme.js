import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#FF8E53',
        },
        secondary: {
            main: '#FE6B8B',
        },
        background: {
            default: '#f5f5f5',
            paper: '#ffffff',
        },
    },
});

export default theme;